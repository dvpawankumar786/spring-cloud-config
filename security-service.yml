eureka:
  instance:
    leaseRenewalIntervalInSeconds: 1
    leaseExpirationDurationInSeconds: 1
  client:
    enabled: false
    serviceUrl:
      defaultZone: ${EUREKA_SERVER:http://localhost:8761/eureka}
    healthcheck:
      enabled: true
    lease:
      duration: 0
    eureka-server-port: 8761
server:
  contextPath: /spring-boot-security-jwt
spring:
  application:
    name: security-service
